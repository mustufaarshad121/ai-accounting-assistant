# Frontend environment (local development example)
# Copy to .env.local and adjust as needed. Only NEXT_PUBLIC_* vars reach the browser.

# Base URL of the FastAPI backend.
NEXT_PUBLIC_API_URL=http://localhost:8000
